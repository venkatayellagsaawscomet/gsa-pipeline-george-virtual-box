import { Test } from 'nodeunit';
declare const _default: {
    'Linux GPU build image': {
        'AWS Deep Learning Container images': {
            'allows passing the account that the repository of the image is hosted in'(test: Test): void;
        };
    };
};
export = _default;
