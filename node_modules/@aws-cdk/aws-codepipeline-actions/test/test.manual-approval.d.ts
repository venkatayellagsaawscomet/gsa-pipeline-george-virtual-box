import { Test } from 'nodeunit';
declare const _default: {
    'manual approval Action': {
        'allows passing an SNS Topic when constructing it'(test: Test): void;
        'renders CustomData and ExternalEntityLink even if notificationTopic was not passed'(test: Test): void;
    };
};
export = _default;
