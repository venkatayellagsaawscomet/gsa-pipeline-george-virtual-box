import { Test } from 'nodeunit';
declare const _default: {
    'BitBucket source Action': {
        'produces the correct configuration when added to a pipeline'(test: Test): void;
    };
    'setting codeBuildCloneOutput=true adds permission to use the connection to the following CodeBuild Project'(test: Test): void;
};
export = _default;
