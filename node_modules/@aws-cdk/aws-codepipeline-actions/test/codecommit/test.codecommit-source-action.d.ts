import { Test } from 'nodeunit';
declare const _default: {
    'CodeCommit Source Action': {
        'by default does not poll for source changes and uses Events'(test: Test): void;
        'does not poll for source changes and uses Events for CodeCommitTrigger.EVENTS'(test: Test): void;
        'polls for source changes and does not use Events for CodeCommitTrigger.POLL'(test: Test): void;
        'does not poll for source changes and does not use Events for CodeCommitTrigger.NONE'(test: Test): void;
        'cannot be created with an empty branch'(test: Test): void;
        'allows using the same repository multiple times with different branches when trigger=EVENTS'(test: Test): void;
        'exposes variables for other actions to consume'(test: Test): void;
    };
};
export = _default;
