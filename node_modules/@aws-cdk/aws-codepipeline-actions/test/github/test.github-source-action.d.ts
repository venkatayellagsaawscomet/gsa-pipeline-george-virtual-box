import { Test } from 'nodeunit';
declare const _default: {
    'GitHub source Action': {
        'exposes variables for other actions to consume'(test: Test): void;
        'always renders the customer-supplied namespace, even if none of the variables are used'(test: Test): void;
        'fails if a variable from an action without a namespace set that is not part of a pipeline is referenced'(test: Test): void;
        'fails if a variable from an action with a namespace set that is not part of a pipeline is referenced'(test: Test): void;
    };
};
export = _default;
