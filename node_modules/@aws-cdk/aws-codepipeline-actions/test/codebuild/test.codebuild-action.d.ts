import { Test } from 'nodeunit';
declare const _default: {
    'CodeBuild action': {
        'that is cross-account and has outputs': {
            'causes an error'(test: Test): void;
        };
        'can be backed by an imported project'(test: Test): void;
        'exposes variables for other actions to consume'(test: Test): void;
    };
};
export = _default;
